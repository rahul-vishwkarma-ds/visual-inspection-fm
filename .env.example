# Copy to .env and fill in — .env is gitignored, never commit real secrets here.

# Optional: override default data/model directories from src/visual_inspection_fm/config.py
# DATA_DIR=./data
# MODELS_DIR=./models

# Optional: experiment tracking, if/when added
# WANDB_API_KEY=
